<!DOCTYPE html>
<html lang="ar" dir="rtl"
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تواصل معنا - DIY</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        :root {
            --primary: #FF6B00;
            --primary-light: #FF8A3D;
            --secondary: #6B48FF;
            --dark: #1A1A2E;
            --light: #F8F9FA;
            --gray: #6C757D;
            --success: #28A745;
            --error: #DC3545;
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            --shadow-sm: 0 1px 3px rgba(0,0,0,0.12);
            --shadow-md: 0 4px 6px rgba(0,0,0,0.1);
            --shadow-lg: 0 10px 20px rgba(0,0,0,0.1);
            --radius-sm: 8px;
            --radius-md: 12px;
            --radius-lg: 16px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Tajawal', sans-serif;
            background-color: var(--light);
            color: var(--dark);
            line-height: 1.7;
            overflow-x: hidden;
        }

        .container {
            width: 100%;
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Header Section */
        .contact-header {
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
            color: white;
            padding: 6rem 0;
            position: relative;
            overflow: hidden;
        }

        .contact-header::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" preserveAspectRatio="none"><path fill="rgba(255,255,255,0.05)" d="M0,0 L100,0 L100,100 Q50,80 0,100 L0,0 Z"></path></svg>');
            background-size: cover;
            background-repeat: no-repeat;
            opacity: 0.5;
        }

        .header-content {
            position: relative;
            z-index: 2;
            text-align: center;
            max-width: 800px;
            margin: 0 auto;
        }

        .header-content h1 {
            font-size: 3.5rem;
            font-weight: 900;
            margin-bottom: 1.5rem;
            line-height: 1.2;
            color: #fff !important;
            text-shadow: 0 2px 8px rgba(0,0,0,0.18);
        }

        .header-content p {
            font-size: 1.25rem;
            opacity: 0.9;
            margin-bottom: 2rem;
        }

        .header-illustration {
            margin-top: 3rem;
            display: flex;
            justify-content: center;
        }

        .header-illustration img {
            max-width: 400px;
            width: 100%;
            filter: drop-shadow(0 10px 20px rgba(0,0,0,0.2));
        }

        /* Main Content */
        .contact-main {
            padding: 5rem 0;
            position: relative;
        }

        .contact-main::before {
            content: '';
            position: absolute;
            top: -50px;
            right: 0;
            width: 100%;
            height: 100px;
            background: var(--light);
            clip-path: polygon(0 0, 100% 50%, 100% 100%, 0% 100%);
        }

        /* Contact Methods */
        .contact-methods {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-bottom: 5rem;
        }

        .method-card {
            background: white;
            border-radius: var(--radius-md);
            padding: 2.5rem;
            text-align: center;
            transition: var(--transition);
            box-shadow: var(--shadow-sm);
            border: 1px solid rgba(0,0,0,0.05);
            position: relative;
            overflow: hidden;
        }

        .method-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-lg);
        }

        .method-card::after {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
        }

        .method-icon {
            width: 80px;
            height: 80px;
            margin: 0 auto 1.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, rgba(255,107,0,0.1) 0%, rgba(107,72,255,0.1) 100%);
            border-radius: 50%;
            font-size: 2rem;
            color: var(--primary);
        }

        .method-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--dark);
        }

        .method-card p {
            color: var(--gray);
            margin-bottom: 1.5rem;
        }

        .method-link {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--primary);
            font-weight: 500;
            text-decoration: none;
            transition: var(--transition);
        }

        .method-link:hover {
            color: var(--secondary);
            transform: translateX(-5px);
        }

        /* Contact Form */
        .contact-form-container {
            background: white;
            border-radius: var(--radius-lg);
            overflow: hidden;
            box-shadow: var(--shadow-lg);
            margin-bottom: 5rem;
            display: grid;
            grid-template-columns: 1fr 1fr;
        }

        .form-illustration {
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 3rem;
            position: relative;
            overflow: hidden;
        }

        .form-illustration::before {
            content: '';
            position: absolute;
            top: -50px;
            right: -50px;
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background: rgba(255,255,255,0.1);
        }

        .form-illustration::after {
            content: '';
            position: absolute;
            bottom: -80px;
            left: -80px;
            width: 250px;
            height: 250px;
            border-radius: 50%;
            background: rgba(255,255,255,0.1);
        }

        .form-illustration img {
            max-width: 100%;
            z-index: 2;
            filter: drop-shadow(0 5px 15px rgba(0,0,0,0.2));
        }

        .contact-form {
            padding: 3rem;
        }

        .form-title {
            font-size: 2rem;
            margin-bottom: 1.5rem;
            color: var(--dark);
            position: relative;
            display: inline-block;
        }

        .form-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            right: 0;
            width: 50px;
            height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
            border-radius: 2px;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: var(--dark);
        }

        .form-control {
            width: 100%;
            padding: 1rem;
            border: 1px solid rgba(0,0,0,0.1);
            border-radius: var(--radius-sm);
            font-family: inherit;
            font-size: 1rem;
            transition: var(--transition);
            background-color: rgba(0,0,0,0.02);
        }

        .form-control:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(255,107,0,0.1);
            background-color: white;
        }

        textarea.form-control {
            min-height: 150px;
            resize: vertical;
        }

        .submit-btn {
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            color: white;
            border: none;
            padding: 1rem 2rem;
            font-size: 1rem;
            font-weight: 500;
            border-radius: var(--radius-sm);
            cursor: pointer;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            box-shadow: 0 4px 15px rgba(107, 72, 255, 0.2);
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(107, 72, 255, 0.3);
        }

        /* Form Message */
        .form-message {
            padding: 1rem;
            border-radius: var(--radius-sm);
            margin-top: 1.5rem;
            display: none;
            animation: fadeIn 0.3s ease;
        }

        .form-message.success {
            background: rgba(40, 167, 69, 0.1);
            color: var(--success);
            border: 1px solid rgba(40, 167, 69, 0.3);
        }

        .form-message.error {
            background: rgba(220, 53, 69, 0.1);
            color: var(--error);
            border: 1px solid rgba(220, 53, 69, 0.3);
        }


        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-15px); }
            100% { transform: translateY(0px); }
        }

        .floating {
            animation: float 4s ease-in-out infinite;
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
            .contact-form-container {
                grid-template-columns: 1fr;
            }
            
            .form-illustration {
                display: none;
            }
        }

        @media (max-width: 768px) {
            .header-content h1 {
                font-size: 2.5rem;
            }
            
            .contact-header {
                padding: 4rem 0;
            }
            
            .contact-main {
                padding: 3rem 0;
            }
            
            .contact-form {
                padding: 2rem;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 0 1rem;
            }
            
            .header-content h1 {
                font-size: 2rem;
            }
            
            .method-card {
                padding: 1.5rem;
            }
            
            .contact-form {
                padding: 1.5rem;
            }
            
            .cta-content h2 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/headerhome') %>
    <%- include('partials/headeraction') %>
    
    <!-- Hero Section -->
    <section class="contact-header">
        <div class="container">
            <div class="header-content">
                <h1>تواصل مع فريق DIY</h1>
                <p>نحن هنا لمساعدتك! سواء كان لديك سؤال، استفسار أو مجرد كلمة لطيفة، فريق الدعم لدينا مستعد للرد عليك في أي وقت.</p>
            </div>
            <div class="header-illustration">
                <img src="https://cdn-icons-png.flaticon.com/512/3062/3062634.png" alt="Contact Illustration" class="floating">
            </div>
        </div>
    </section>

    <!-- Main Content -->
    <main class="contact-main">
        <div class="container">
            <!-- Contact Methods -->
            <div class="contact-methods">
                <div class="method-card">
                    <div class="method-icon">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <h3>البريد الإلكتروني</h3>
                    <p>راسلنا مباشرة وسنرد عليك خلال 24 ساعة</p>
                    
<a href="mailto:diyfors@gmail.com" class="method-link">
    diyfors@gmail.com
    <i class="fas fa-arrow-left"></i>
</a>
                </div>
                
                <div class="method-card">
                    <div class="method-icon">
                        <i class="fas fa-phone-alt"></i>
                    </div>
                    <h3>الدعم الهاتفي</h3>
                    <p>متاح من الأحد إلى الخميس من 9 صباحًا إلى 5 مساءً</p>
                    <a href="tel:+249902707192" class="method-link">
                        +249902707192
                        <i class="fas fa-arrow-left"></i>
                    </a>
                </div>
               

            <!-- Contact Form -->
            <div class="contact-form-container">
                <div class="form-illustration">
                    <img src="https://cdn-icons-png.flaticon.com/512/1348/1348057.png" alt="Form Illustration">
                </div>
                
                <div class="contact-form">
                    <h2 class="form-title">أرسل رسالتك</h2>
                    <form id="contactForm">
                        <div class="form-group">
                            <label for="name" class="form-label">اسمك الكامل</label>
                            <input type="text" id="name" name="name" class="form-control" placeholder="أدخل اسمك" required>
                        </div>
                        
                        <div class="form-group">
                            <label for="email" class="form-label">البريد الإلكتروني</label>
                            <input type="email" id="email" name="email" class="form-control" placeholder="أدخل بريدك الإلكتروني" required>
                        </div>
                        
                        <div class="form-group">
                            <label for="subject" class="form-label">موضوع الرسالة</label>
                            <input type="text" id="subject" name="subject" class="form-control" placeholder="ما هو موضوع رسالتك؟" required>
                        </div>
                        
                        <div class="form-group">
                            <label for="message" class="form-label">رسالتك</label>
                            <textarea id="message" name="message" class="form-control" placeholder="أكتب رسالتك هنا..." required></textarea>
                        </div>
                        
                        <button type="submit" class="submit-btn">
                            إرسال الرسالة
                            <i class="fas fa-paper-plane"></i>
                        </button>
                    </form>
                    
                    <div id="formMessage" class="form-message"></div>
                </div>
            </div>
        </div>
    </main>


    <%- include('partials/footer') %>

    <script>
        document.getElementById('contactForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const form = e.target;
            const formData = new FormData(form);
            const data = Object.fromEntries(formData);
            const messageDiv = document.getElementById('formMessage');
            
            // Save original button content
            const submitButton = form.querySelector('button');
            const originalButtonContent = submitButton.innerHTML;
            
            // Show loading state
            submitButton.innerHTML = '<i class="fas fa-spinner fa-spin"></i> جاري الإرسال...';
            submitButton.disabled = true;
            
            try {
                const response = await fetch('/contact/send', {
                    method: 'POST',
                    headers: { 
                        'Content-Type': 'application/json',
                        'X-Requested-With': 'XMLHttpRequest'
                    },
                    body: JSON.stringify(data)
                });
                
                const result = await response.json();
                
                // Show message
                messageDiv.style.display = 'block';
                
                if (response.ok) {
                    messageDiv.textContent = 'تم إرسال رسالتك بنجاح! سنقوم بالرد عليك في أقرب وقت ممكن.';
                    messageDiv.className = 'form-message success';
                    form.reset();
                } else {
                    messageDiv.textContent = result.message || 'حدث خطأ أثناء إرسال الرسالة، يرجى المحاولة مرة أخرى.';
                    messageDiv.className = 'form-message error';
                }
            } catch (error) {
                messageDiv.style.display = 'block';
                messageDiv.textContent = 'حدث خطأ في الاتصال بالخادم، يرجى المحاولة لاحقًا.';
                messageDiv.className = 'form-message error';
                console.error('Error:', error);
            } finally {
                // Restore original button state
                submitButton.innerHTML = originalButtonContent;
                submitButton.disabled = false;
                
                // Hide message after 5 seconds
                setTimeout(() => {
                    messageDiv.style.display = 'none';
                }, 5000);
            }
        });
    </script>
</body>
</html>